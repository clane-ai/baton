# Baton app: status, week of 24 September 2026

Owner: clane-baton-f6 (UI and UX). Milestones per the SoW: M1 design, M2 build and UAT ready, M3 UAT passed, M4 handover.

## This week

- M1 signed off on 24 September with one change: the visual language is monday.com-style (white workspace on a
  light grey canvas, Figtree, saturated status pills, one purple-blue accent). Spec: `docs/superpowers/specs/2026-09-24-baton-app-m1-design.md`;
  mock-up: `docs/design/mockups/baton-app-m1.html`.
- M2 build, all 13 planned tasks done (`docs/superpowers/plans/2026-09-24-baton-app-m2.md`): rail and real routes;
  Inbox with tiles, filters, grouped boards and keyboard; Item with sources, document pane, policy summary, activity
  and the decision bar (approve, reject with reason, retry, answer, browser-local drafts, next-item flow); Runs and
  Run with steps, artefacts, activity and the restyled graph; Documents; Activity; Spend; operator console under
  `/ops`; the hash-tab dashboard retired. 36 unit tests over the pure helpers; typecheck clean; every route answers.
- Engine asks 1 to 8 shipped by the engine owner the same day (edge function v15). One defect found and fixed on the
  way (the inbox route's questions branch returned 500). The app keeps a fallback for `/admin/inbox`.

## Review outcome (same day)

A fresh whole-branch review found no Critical and four Important issues, all fixed with tests first:
the inbox fallback no longer masks an auth or config error as an empty queue (falls back only on 404/5xx);
the email parser decodes UTF-8 quoted-printable and base64 bodies, reads lower-case header names and nested
multipart/alternative; muted and secondary text moved to the 5.6:1 token and the done/working pills to ink on
tint for contrast. 41 unit tests pass. Deferred minors, recorded in the plan ledger: rail badge count differs from
the Inbox tiles; unknown item key shows an error box rather than an empty state; a missing PDF shows the JSON error
inside the iframe; `a`/`r` shortcuts and scroll-into-view on `j`/`k` not built; ARIA roles on the inbox rows and
the nested link in activity rows; server-side trim of a whitespace reason; `tsconfig.tsbuildinfo` tracked; the run
screen's per-step artefact fan-out; no run filter on Documents.

## Direction change (same evening)

The user decided the app becomes part of Clane AI, using Clane's design system, fonts and components. The M2
app is now the reference implementation; the port plan is `docs/superpowers/specs/2026-09-24-baton-app-in-clane.md`
and targets the Clane platform's web client per the Clane architect's placement.

## Workflow screens in Clane (late evening)

The screens are rebuilt as the operational part of Clane's Workflow section, per the architect's ruling:
inside the main client beside the workflow studio, under `/app/build/workflows`, on Clane's design system,
fonts and copy catalogue. They are staged in `packages/approvals` until the platform worktree exists.

| Built | Screens and parts |
| --- | --- |
| Screens | Approvals, Work item, Runs, Run, Documents, Activity, Spend |
| Parts | business-document view with provenance, source documents with PDF by authenticated blob, decision bar, run graph, activity sentences |
| Data | one facade over `/api/workflow-ops`, keyset paging, polling that pauses when the tab is hidden |

| Check | Result |
| --- | --- |
| Staging suite | 142 of 142 |
| Platform check: the platform's own `tsc` | no error added (8 exist on main) |
| Platform check: the platform's own Jest | 161 of 161, no React warnings |

Findings that changed the build: the platform runs Jest, not Vitest; its shared Tabs, Input, FilterBar,
ApprovalCard and Drawer are not keyboard-operable, so the area has accessible local versions; the shared
barrel needs a type declaration or the platform build fails; a deep link would lose its path on the second
refresh without a one-line router guard (in `PATCHES.md`).

Waiting: user approval to create the platform worktree `feat/clane-workflow-module`; the gateway agent's
choice of the final path segment and the platform-side edits in `PATCHES.md`; no push until the repository
question is settled.

## Blocked or waiting

- Visual verification in a browser: the Chrome tooling was not connected in the build session, so screens were
  verified by API shape, typecheck and route checks, not by screenshot. First UAT pass should start with a look at
  `/inbox`, `/inbox/[key]` and `/runs/[key]` at 1440 px.
- Provenance markers (ask 6): the slot is built; real values arrive with the next P2P run that carries `_provenance`.

## Next week

- UAT over the script at the end of the plan, on runs p2p-101 to p2p-122 plus a fresh run with provenance.
- Fixes from UAT; then M3 sign-off and the M4 handover notes.
